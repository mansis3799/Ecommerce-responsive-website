import React from 'react';
import Base from "../core/Base";

export default function Profile(){
    return(
        <Base title="User's Profile" description="A page for user's profile">Profile!!</Base>
    )
}